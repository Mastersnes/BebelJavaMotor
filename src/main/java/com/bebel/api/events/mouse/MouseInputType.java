package com.bebel.api.events.mouse;

/**
 * Liste des differents evenements souris
 */
public enum MouseInputType {
        NONE,
        DOWN, UP,
        DRAG, MOVE,
        SCROLL, CLICK,
        ENTER, HOVER, EXIT,
        FOCUS, UNFOCUS;
}